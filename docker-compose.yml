services:
  db:
    image: mysql:8
    container_name: shortlink_db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASS}
      MYSQL_DATABASE: ${DB_NAME}
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_PASS}"]
      interval: 10s
      timeout: 5s
      retries: 5

  frontend:
    build: ./frontend
    expose:
      - "3000"
    networks:
      - shortlink-network

  backend:
    build: ./backend
    expose:
      - "5000"
    networks:
      - shortlink-network

  nginx:
    image: nginx:alpine
    container_name: nginx_proxy
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - shortlink-network
    depends_on:
      - frontend
      - backend

  ngrok:
    image: ngrok/ngrok:latest
    container_name: ngrok_tunnel
    restart: always
    environment:
      - NGROK_AUTHTOKEN=DÁN_TOKEN_THẬT_CỦA_BẠN_VÀO_ĐÂY
    command:
      - "http"
      - "nginx:80" # Ngrok gọi thẳng tên service "nginx" ở cổng 80
    networks:
      - shortlink-network
    depends_on:
      - nginx

networks:
  shortlink-network:
    driver: bridge

volumes:
  db_data:
