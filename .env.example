# Copy this file to .env and fill in your values
PORT=3000
DB_HOST=db          # 'db' is the docker-compose service name (not localhost)
DB_USER=root
DB_PASS=your_password_here
DB_NAME=shortlink
BASE_URL=http://localhost:3000
